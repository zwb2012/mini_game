---
paths:
  - "server/src/**"
---

# 服务端代码规则

- API 端点必须返回统一响应格式 `{ success, data?, error? }`
- 敏感凭据通过环境变量注入，禁止硬编码
- 支付/登录验证在服务端完成，不信任客户端输入
- 每个路由文件对应一个业务领域
- 公共路由逻辑抽取为中间件
- 数据库操作封装在独立的数据访问层
- 所有外部 API 调用（微信支付验证等）必须有错误处理和超时
