{
  "name": "backend-base",
  "version": "0.1.0",
  "private": true,
  "description": "小游戏后端服务骨架 — Express + TypeScript",
  "scripts": {
    "dev": "npx ts-node src/index.ts",
    "check:types": "npx tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/express": "^4.17.0",
    "ts-node": "^10.9.0"
  }
}
