# Game Studio Agent Architecture

This project's workspace instructions now live in **[`AGENTS.md`](AGENTS.md)**.

DeepSeek Harness reads `AGENTS.md` (this Claude Code convention is retained as a
one-line pointer only, to avoid loading the same instructions twice). Open
`AGENTS.md` for the studio coordination rules, coding standards, context
management, skills usage, and role-delegation guidance.
