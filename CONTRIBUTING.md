# 贡献指南

感谢你对这个模板仓库感兴趣。

这不是一个普通代码仓库，而是一个围绕“微信小游戏自动化产品流水线”的模板仓库。因此，贡献内容时请优先维护：
- 流程一致性
- 门禁清晰度
- 中文文档输出规则
- 模板与运行态事实源边界

---

## 一、适合贡献的内容
欢迎贡献：
- 新阶段模板
- 新阶段 skill
- 工作流规则完善
- 操作说明改进
- 示例项目产物
- 更清晰的门禁协议
- 文档导航与可读性增强

---

## 二、提交前请先判断内容属于哪一层
### 1. 属于 `skills/`
当你的改动是：
- 一个可复用的阶段能力
- 一个稳定的技能文档
- 一个新的总控或叶子技能

### 2. 属于 `specs/workflows/automation/`
当你的改动是：
- 状态机
- 门禁
- 停止条件
- 启动协议
- 操作说明
- 语言规则

### 3. 属于 `specs/_templates/`
当你的改动是：
- 某个阶段的模板结构
- 统一的 frontmatter
- 阶段文档字段定义

### 4. 属于 `portfolio/`
当你的改动是：
- 组合层运行态样例
- WIP 规则样例
- 决策记录结构

### 5. 属于 `specs/projects/`
当你的改动是：
- 示例项目产物链
- dry run 示例
- 启动样例

---

## 三、必须遵守的规则
1. 所有文档型产物默认使用中文。
2. 不要自动越过这三个门禁：
   - 方向选择
   - PRD 审批
   - 提审包完成
3. 不要把真实微信提审动作纳入自动执行范围。
4. 不要让运行态事实源分散到多个文件中互相冲突。
5. `portfolio/projects.yaml`、`portfolio/wip-rules.yaml`、`portfolio/decision-log.md` 与 `portfolio/portfolio-board.md` 的职责必须清晰分离。

---

## 四、推荐提交流程
1. 先确认你改动的是哪一层
2. 确认中文输出规则没有被破坏
3. 确认模板、技能、规则之间没有路径冲突
4. 如有新增阶段或门禁，补齐：
   - 对应 skill
   - 对应模板
   - 对应规则说明
   - 必要的使用说明
5. 尽量附带一个最小 dry run 或使用示例

---

## 五、Pull Request 建议说明
提交 PR 时建议包含：
- 改动属于哪一层（skills / workflows / templates / portfolio / projects）
- 为什么需要这个改动
- 是否改变了门禁、状态机或事实源边界
- 是否影响中文输出规则
- 如何验证

---

## 六、不建议的贡献方式
- 只改 README，不改真实规则文件
- 新增一个 skill，但没有对应模板或门禁说明
- 在多个运行态文件里重复写同一份事实源
- 把主要文档内容改成英文
- 引入会自动执行真实提审动作的流程

---

## 七、如果你不确定该放哪
优先判断：
- 这是“人类操作说明”吗？→ `specs/workflows/automation/`
- 这是“阶段产物模板”吗？→ `specs/_templates/`
- 这是“运行态事实源”吗？→ `portfolio/` 或 `specs/projects/`
- 这是“可复用技能”吗？→ `skills/`

如果仍然不确定，先在 PR 描述里明确你的判断依据。
