# 模板仓库、项目仓库与组合层关系

## 一、三层结构
### 1. 模板仓库
作用：保存可复用的自动化流水线母版。

内容包括：
- workflow / rules / schema
- 所有可复用技能
- 阶段模板
- 子代理提示规范

它不保存具体小游戏产品代码。

### 2. 项目仓库
每个小游戏一个独立仓库。

内容包括：
- 项目代码
- 项目文档
- 资源素材
- 测试结果
- 提审准备材料

多个项目仓库之间是兄弟关系，不共享业务目录。

### 3. 组合层 / portfolio 仓库
作用：跨项目管理。

内容包括：
- 项目索引
- 当前状态
- WIP 规则
- 优先级
- 决策记录

## 二、推荐关系
- 模板仓库：定义“怎么做”
- 项目仓库：承载“具体产出”
- 组合层：决定“先做谁、做到哪、谁该停”

## 三、项目进度管理方式
每个项目仓库维护一份自己的状态文件，例如：
- `specs/projects/<slug>/state.yaml`

组合层汇总多个项目状态，例如：
- `portfolio/projects.yaml`
- `portfolio/portfolio-board.md`
- `portfolio/decision-log.md`

## 四、组合层单一事实源（SSOT）
为避免状态漂移，组合层文件职责固定如下：

- `portfolio/projects.yaml`：**唯一运行态数据源**。记录项目列表、当前状态、下一步和阻塞项。
- `portfolio/wip-rules.yaml`：**唯一 WIP 与优先级规则源**。记录并行上限和排序原则。
- `portfolio/portfolio-board.md`：**展示视图**。面向人类阅读的汇总看板，可由前两者生成或同步更新。
- `portfolio/decision-log.md`：**决策记录**。记录关键调整与人为判断。

因此：
- 不应在多个运行态文件里重复维护同一份 WIP 规则作为事实源。
- `portfolio-board.md` 负责展示，不作为 WIP 规则的唯一来源。

## 五、为什么不把多个项目放进同一个代码仓库
1. 业务代码容易互相污染
2. 上架节奏不同，分支和版本管理会变乱
3. 测试、资源、构建产物会相互干扰
4. 自动化流水线很难清晰界定每个项目的边界

因此推荐：
- **一个模板仓库**
- **一个组合层仓库**
- **多个项目仓库**
